
# ── GSD (auto-generated) ──
.gsd
.gsd-worktrees/
.gsd-id
.mcp.json
.gsd-backups/

# ── OS ──
.DS_Store
Thumbs.db
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*

# ── Editors ──
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ── Environment ──
.env
.env.*
!.env.example

# ── Dependencies & Build ──
node_modules/
.next/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# ── Language runtimes ──
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
tmp/
.cache/

# ── Logs ──
*.log

# ── Test Artifacts ──
.test*db
.test*-shm
.test*-wal
.test*-journal
.test*db-journal
test-results/
playwright-report/
coverage/

# ── User uploads (created at runtime) ──
public/uploads/

# ── Documentation artifacts ──
# Durable product documentation, requirements, and selected UAT evidence under
# docs/ are versioned. Only generated or local tooling artifacts stay ignored.
docs/.gsd/
docs/uat/generated/
docs/gsd/
docs/*.zip

# ── Runtime directories ──
.trading-journal/
.bg-shell/
.impeccable/

# GSD compat bad files (generated on compat parse failure)
.gsd/.compat.json.bad-*
snapshots/
